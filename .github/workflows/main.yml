name: Get Price Action

on:
  schedule:
    - cron: "15 * * * *"

jobs:
  getPrice:
    runs-on: ubuntu-latest
    steps:
      - name: Get Price
        run: |
          curl https://chp.mananjoshi.me/api/get-price
        shell: bash
